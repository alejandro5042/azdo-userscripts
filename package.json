{
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "eslint": "^5.3.0"
  },
  "scripts": {
    "build": "npm run lint",
    "lint": "eslint --ignore-path .gitignore .",
    "lint-fix": "eslint --fix --ignore-path .gitignore ."
  }
}
