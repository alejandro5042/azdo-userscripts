{
    "private": true,
    "dependencies": {},
    "devDependencies": {
        "@ni/eslint-config": "^1.0.0-alpha.2",
        "eslint": "^7.6.0",
        "eslint-plugin-import": "^2.22.0"
    },
    "scripts": {
        "build": "npm run lint",
        "lint": "eslint --ignore-path .gitignore .",
        "lint-fix": "eslint --fix --ignore-path .gitignore ."
    }
}
